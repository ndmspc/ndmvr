// These are type declarations for props that exist at runtime
// but are missing from @react-three/uikit and @react-three/uikit-default type definitions
// Instead of module augmentation, we'll use type assertions in the components

export {};
